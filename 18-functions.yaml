- name: functions
  hosts: local
  connection: local
  tasks:
  - name: default variable
    ansible.builtin.debug:
      msg: "Hello, {{ Name | default(Sindhu) }}" #default is an inbuilt function in ansible